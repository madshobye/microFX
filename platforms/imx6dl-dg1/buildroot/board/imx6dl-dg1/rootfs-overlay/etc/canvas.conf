# Global runtime configuration (legacy CANVAS_* names remain compatible).
# Production defaults avoid persistent writes to the SD root filesystem.
CANVAS_SSH=0
CANVAS_DEBUG=0
CANVAS_PERSIST_LOGS=0
CANVAS_FAIL_FAST=1
